/node_modules
/.next
/out
/build
.env*.local
next-env.d.ts
*.tsbuildinfo
.vercel

# .env.production só tem valores NEXT_PUBLIC_ (públicos) — re-incluir
# (o .gitignore raiz exclui .env.* ; isto anula isso só para este ficheiro)
!.env.production
